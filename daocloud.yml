image: daocloud/ci-golang:1.4

#services:
#  - redis

# using default docker-link env
#env:
#  - REDIS_PORT_6379_TCP_PROTO = "tcp"
#  - REDIS_PASSWORD = ""

install:
  - sudo apt-get update

before_script:
  - mkdir -p /gopath/app/golang-app/src
  - mv ./* /gopath/app/golang-app/src

script:
  - export GOPATH=/gopath/app
#  - go get -t golang-app
#  - go test golang-app
